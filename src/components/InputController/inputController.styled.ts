import styled from 'styled-components';

export const DivController = styled.div`
    display: flex;
    flex-direction: column;
    font-size: 1.2rem;
    font-weight: 700;
`;

export const InputField = styled.input`
    padding: 0.5rem;
    border-radius: 0.5rem;
`;
